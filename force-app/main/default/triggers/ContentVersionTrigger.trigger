trigger ContentVersionTrigger on ContentVersion (after insert) {
    //if(Trigger.isInsert && Trigger.isafter && ContentVersionTriggerHandler.isDisable){
    //    ContentVersionTriggerHandler.afterinserMenthod();
    //}
}